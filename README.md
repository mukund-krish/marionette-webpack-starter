# Basic Marionette + webpack project starter #

## Hot to setup ##

## How to run in dev mode ##

```
$> ./node_modules/.bin/webpack-dev-server --inline
```

don't forget the `--inline` option to enable automatic browser reload.
